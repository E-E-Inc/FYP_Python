setup:
  - python38
  - gcc

install:
  - python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt

start:
  - . /opt/venv/bin/activate && python ./src/flask-server.py & python ./src/image-processing-microservice.py
