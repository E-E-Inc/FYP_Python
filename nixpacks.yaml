setup:
  - python38
  - gcc

install:
  - python -m venv myenv && myenv\Scripts\activate && pip install -r requirements.txt

start:
  - myenv\Scripts\activate && python ./src/main.py & python ./src/microservice.py
